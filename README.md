# CTG_Analysis

## Running with CTG_Dataset.csv

Place `CTG_Dataset.csv` in the repository root and run:

```
python Analysis.py
```

When present, the script loads `CTG_Dataset.csv` and uses `NSP` as the output label.
